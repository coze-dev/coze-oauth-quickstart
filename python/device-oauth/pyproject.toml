[project]
name = "coze-device-oauth"
version = "0.1.0"
description = "Coze Device OAuth Quick Start"
requires-python = ">=3.8"
dependencies = [
    "cozepy==0.11.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
